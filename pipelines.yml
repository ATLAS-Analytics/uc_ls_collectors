# - pipeline.id: main
#   path.config: "/usr/share/logstash/pipeline"

# - pipeline.id: rucio
#   path.config: "/usr/share/logstash/rucio"
#   pipeline.workers: 1
#   # queue.type: persisted

# - pipeline.id: atlasrift
#   path.config: "/usr/share/logstash/atlasrift"
#   pipeline.workers: 1
#   # queue.type: persisted

# - pipeline.id: benchmarks
#   path.config: "/usr/share/logstash/pilot/PandaPilot.conf"
#   pipeline.workers: 1

- pipeline.id: memory-monitor
  path.config: "/usr/share/logstash/pilot/PilotMonitor.conf"
  pipeline.workers: 1

# queue.type: persisted
- pipeline.id: xcache
  path.config: "/usr/share/logstash/xcache/xCacheMonitoringCollector.conf"
  pipeline.workers: 1

  # queue.type: persisted
- pipeline.id: xcache-gstream
  path.config: "/usr/share/logstash/xcache/xCache_gStreamCollector.conf"
  pipeline.workers: 1

  # queue.type: persisted
- pipeline.id: stashcp
  path.config: "/usr/share/logstash/stashcp/StashCP.conf"
  pipeline.workers: 1
  # queue.type: persisted
