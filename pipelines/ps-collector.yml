- pipeline.id: intake
  config.string: |
    input { 
      http {
        host => "0.0.0.0"
        port => "80"
      }
    }
    output { pipeline { send_to => ["ps-collector", "ps-status"] } }
- pipeline.id: ps-collector
  path.config: "/usr/share/logstash/configs/ps-collector.conf"
  pipeline.workers: 1
- pipeline.id: ps-status
  path.config: "/usr/share/logstash/configs/ps-status.conf"
  pipeline.workers: 1
#   queue.type: persisted
#   queue.max_bytes: 4gb
